{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Relay": {
    // Where matches live. Empty means in memory, which is right for a local run and wrong for
    // anything a player might come back to: an Anytime match runs over days and the seats and tokens
    // in it cannot be reissued. Set a file path to deploy, or Relay__Database in the environment.
    "Database": ""
  }
}
